@extends('layouts.app')
@section('content')
<div class="customer-shell"><header class="customer-nav"><a class="brand" href="/">Kuakata<span>Stay</span></a><a href="{{ route('customer.dashboard') }}">← My bookings</a></header><main><p class="eyebrow">BOOKING DETAILS</p><h1>{{ $booking->property->name ?? 'Hotel' }}</h1><div class="detail-card"><p><strong>Booking:</strong> #{{ $booking->booking_number }}</p><p><strong>Stay:</strong> {{ optional($booking->check_in)->format('d M Y') }} — {{ optional($booking->check_out)->format('d M Y') }} ({{ $booking->nights }} nights)</p><p><strong>Status:</strong> {{ strtoupper(str_replace('_',' ',$booking->status)) }}</p><p><strong>Total:</strong> ৳{{ number_format((float)$booking->total,2) }}</p><div class="detail-actions"><a href="{{ route('booking.invoice',$booking) }}">View invoice</a>@if($booking->status==='pending_payment')<a href="{{ route('booking.payment',$booking) }}">Complete payment</a>@endif</div></div></main></div>
@endsection