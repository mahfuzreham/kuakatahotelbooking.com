@extends('layouts.app')
@section('content')
<div class="container py-4"><div class="d-flex justify-content-between align-items-center mb-3"><h2>Refund Management</h2><a href="{{ route('dashboard.admin') }}" class="btn btn-outline-secondary">Dashboard</a></div>
<form class="mb-3"><select name="status" onchange="this.form.submit()" class="form-select" style="max-width:220px"><option value="">All statuses</option>@foreach(['pending_review','approved','rejected','processed'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ ucfirst(str_replace('_',' ',$s)) }}</option>@endforeach</select></form>
<div class="table-responsive"><table class="table table-hover"><thead><tr><th>ID</th><th>Booking</th><th>Amount</th><th>Status</th><th>Reason</th><th>Action</th></tr></thead><tbody>@forelse($refunds as $refund)<tr><td>#{{ $refund->id }}</td><td>#{{ $refund->booking_id }}</td><td>{{ number_format($refund->amount,2) }}</td><td><span class="badge bg-secondary">{{ $refund->status }}</span></td><td>{{ $refund->reason }}</td><td>@if($refund->status==='pending_review')<form class="d-inline" method="POST" action="{{ route('admin.refunds.approve',$refund) }}">@csrf<button class="btn btn-sm btn-success">Approve</button></form><form class="d-inline" method="POST" action="{{ route('admin.refunds.reject',$refund) }}">@csrf<button class="btn btn-sm btn-danger">Reject</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="text-center">No refund requests found.</td></tr>@endforelse</tbody></table></div>{{ $refunds->links() }}</div>
@endsection