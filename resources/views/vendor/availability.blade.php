@extends('layouts.app')
@section('content')
<div class="vendor-app"><div class="vendor-shell"><aside class="vendor-sidebar"><a class="vendor-brand" href="{{ route('vendor.dashboard') }}">Kuakata<span>Stay</span></a><nav class="vendor-nav"><a href="{{ route('vendor.dashboard') }}">Dashboard</a><a href="{{ route('vendor.properties.index') }}">Properties</a><a href="{{ route('vendor.bookings.index') }}">Bookings</a><a class="active" href="{{ route('vendor.availability.edit',$roomType) }}">Availability</a><a href="{{ route('vendor.payouts.index') }}">Wallet & Payouts</a></nav></aside><section class="vendor-content"><header class="vendor-topbar"><strong>Availability</strong><a class="vendor-btn secondary" href="{{ route('vendor.rooms.index',$roomType->property) }}">Back to rooms</a></header><main class="vendor-main"><p class="vendor-eyebrow">PRICING & INVENTORY</p><h1 class="vendor-title">{{ $roomType->name }}</h1><p class="vendor-muted">{{ $roomType->property->name }} · Base price ৳{{ number_format((float)$roomType->base_price,2) }} · Inventory {{ $roomType->inventory }}</p>@if(session('success'))<div class="vendor-alert" style="margin-top:16px">{{ session('success') }}</div>@endif@if($errors->any())<div class="vendor-errors" style="margin-top:16px">{{ $errors->first() }}</div>@endif<div class="vendor-card vendor-section"><h2>Update a date range</h2><form class="vendor-form" method="POST" action="{{ route('vendor.availability.update',$roomType) }}">@csrf<div class="vendor-form-grid"><div class="vendor-field"><label>From</label><input type="date" name="from" value="{{ now()->toDateString() }}" required></div><div class="vendor-field"><label>To</label><input type="date" name="to" value="{{ now()->addDays(7)->toDateString() }}" required></div><div class="vendor-field"><label>Available inventory</label><input type="number" name="available_inventory" min="0" value="{{ $roomType->inventory }}" required></div><div class="vendor-field"><label>Price per night (BDT)</label><input type="number" name="price" min="0" step="0.01" value="{{ $roomType->base_price }}" required></div><div class="vendor-field"><label>Minimum stay</label><input type="number" name="minimum_stay" min="1" max="30" value="1" required></div><label class="settings-toggle"><input type="checkbox" name="is_closed" value="1"><span>Close dates for booking</span></label></div><button class="vendor-btn" type="submit">Save availability</button></form></div><div class="vendor-section"><div class="vendor-section-head"><h2>Next 14 days</h2></div><div class="vendor-table-wrap"><table class="vendor-table"><thead><tr><th>Date</th><th>Inventory</th><th>Price</th><th>Minimum stay</th><th>Status</th></tr></thead><tbody>@forelse($availability as $day)<tr><td>{{ $day->date->format('d M Y') }}</td><td>{{ $day->available_inventory }}</td><td>৳{{ number_format((float)$day->price,2) }}</td><td>{{ $day->minimum_stay }}</td><td><span class="vendor-badge">{{ $day->is_closed ? 'CLOSED' : 'OPEN' }}</span></td></tr>@empty<tr><td colspan="5" class="vendor-muted">No custom availability set yet. Base inventory and price will apply.</td></tr>@endforelse</tbody></table></div></div></main></section></div></div>
@endsection
