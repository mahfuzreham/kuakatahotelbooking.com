@extends('layouts.app')
@section('content')<div class="customer-shell"><header class="customer-nav"><a class="brand" href="/">Kuakata<span>Stay</span></a><div><a href="{{ route('vendor.properties.index') }}">My properties</a> <form method="POST" action="{{ route('logout') }}" style="display:inline">@csrf<button class="link-button">Sign out</button></form></div></header><main><p class="eyebrow">VENDOR CENTER</p><h1>{{ $vendor?->business_name ?? 'Become a partner' }}</h1>@if(!$vendor)<div class="detail-card"><p>You have not submitted a vendor application.</p><a href="{{ route('vendor.register') }}">Register as vendor</a></div>@else<div class="customer-stats"><div><small>Application status</small><strong>{{ strtoupper($vendor->status) }}</strong></div><div><small>Verification</small><strong>{{ strtoupper($vendor->verification_status) }}</strong></div></div>@if($vendor->status==='approved')<p><a href="{{ route('vendor.properties.index') }}">Manage properties →</a></p>@else<p class="muted">Your application is awaiting admin approval. Property management unlocks after approval.</p>@endif@endif</main></div>@endsection