@extends('layouts.app')
@section('content')<div class="customer-shell"><header class="customer-nav"><a class="brand" href="/">Kuakata<span>Stay</span></a><a href="{{ route('vendor.dashboard') }}">← Vendor center</a></header><main><p class="eyebrow">PROPERTY MANAGEMENT</p><h1>My hotels & properties</h1><p><a class="detail-actions" href="{{ route('vendor.properties.create') }}">Add property</a></p><div class="booking-list">@forelse($properties as $property)<div class="booking-item"><div><strong>{{ $property->name }}</strong><p>{{ $property->city }} · {{ ucfirst($property->type) }}</p></div><div><span class="booking-badge">{{ strtoupper($property->status) }}</span></div></div>@empty<div class="detail-card">No properties yet. Add your first hotel.</div>@endforelse</div></main></div>@endsection